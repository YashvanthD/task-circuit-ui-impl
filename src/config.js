/**
 * App Configuration
 * Re-exports from config folder for backward compatibility.
 *
 * @module config
 * @deprecated Import from './config/' instead
 */

// Re-export everything from the config folder
export * from './config/index';

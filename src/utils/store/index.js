/**
 * Store Index - Export all store utilities
 * @module utils/store
 */

export {
  dataStore,
  normalizeToSnakeCase,
  toSnakeCaseKeys,
  getField,
  getId,
  normalizeNotification,
  normalizeAlert,
  normalizeTask,
  normalizeUser,
  normalizeMessage,
  normalizeConversation,
  normalizePond,
} from './dataStore';

export { default } from './dataStore';
